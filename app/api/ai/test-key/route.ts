import { NextRequest, NextResponse } from "next/server"; export async function POST(request: NextRequest) { const { apiKey, provider } = await request.json(); await new Promise(resolve => setTimeout(resolve, 2000)); const isValid = Math.random() > 0.2; return NextResponse.json({ success: isValid, provider, latency: Math.floor(Math.random() * 500) + 100, model: provider === "openai" ? "gpt-4" : provider === "claude" ? "claude-3" : "gemini-pro", timestamp: new Date().toISOString() }); }
