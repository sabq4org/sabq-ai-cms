import { NextRequest, NextResponse } from "next/server"; export async function POST(request: NextRequest) { const interaction = await request.json(); const logEntry = { id: Date.now().toString(), timestamp: new Date().toISOString(), userId: interaction.userId, articleId: interaction.articleId, action: interaction.action, model: interaction.model, inputTokens: Math.floor(Math.random() * 1000) + 50, outputTokens: Math.floor(Math.random() * 500) + 25, cost: (Math.random() * 0.05).toFixed(4), success: true }; console.log("AI Interaction logged:", logEntry); return NextResponse.json({ success: true, data: logEntry }); } export async function GET() { const mockLogs = [ { id: "1", timestamp: new Date().toISOString(), userId: "user1", action: "generate_title", model: "gpt-4", cost: 0.002 }, { id: "2", timestamp: new Date().toISOString(), userId: "user2", action: "summarize", model: "gpt-3.5-turbo", cost: 0.001 } ]; return NextResponse.json({ success: true, data: mockLogs }); }
