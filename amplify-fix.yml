version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - echo "🔧 Applying Prisma fix for AWS Lambda using script..."
        - chmod +x scripts/fix-prisma-aws-lambda.sh
        - ./scripts/fix-prisma-aws-lambda.sh
        - echo "✅ Prisma AWS Lambda fix completed"
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
